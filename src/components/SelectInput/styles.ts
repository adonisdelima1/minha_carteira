import styled from 'styled-components'; 

export const Container = styled.div`
    > select {
        // padding de 7px em cima e embaixo e 10px em cada lado 
        padding: 7px 10px; 

        // border-radius arredonda os vértices/quinas/cantos
        border-radius: 5px;

        // Margem apenas à esquerda 
        margin-left: 7px;
    }
`;